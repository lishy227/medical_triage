# =============================================================================
# 医疗导诊系统 - Python 依赖
# =============================================================================
# 仅列出直接依赖（顶层 import），不包含传递依赖
# 安装: pip install -r requirements.txt
# =============================================================================

# ---- Web 框架 & 服务器 ----
flask==3.0.3
flask-cors==4.0.1
waitress==3.0.0

# ---- LLM / AI ----
openai==2.29.0

# ---- 数据验证 & 配置 ----
pydantic==2.12.5
pydantic-settings==2.2.1
python-dotenv==1.2.2

# ---- 数据库 ----
SQLAlchemy==2.0.30
PyMySQL==1.1.1

# ---- 认证 & 安全 ----
PyJWT==2.8.0
bcrypt==4.1.3

# ---- 工具 ----
tenacity==9.1.4

# =============================================================================
# RAG 增强（可选，取消注释以启用向量检索）
# =============================================================================
# chromadb==0.5.0
# sentence-transformers==2.7.0

# =============================================================================
# 会话持久化（可选，取消注释以启用 Redis 多进程共享会话）
# =============================================================================
# redis==5.2.1
